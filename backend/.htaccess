# Nothing in here is ever requested directly — only included by PHP scripts
# in the web root. Block direct HTTP access entirely (Apache 2.4 + 2.2).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
